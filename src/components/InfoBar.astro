<div id="info-bar">
	<div class="container">
		<p>
			<img class="icon" src="/images/icons/telefono.svg" alt="Telefono" />
			Línea de atención <a href="tel:+543498420403">+54 3498-42-0403</a>
		</p>
		<p>
			<img class="icon" src="/images/icons/sirena.svg" alt="Sirena" />
			Línea de urgencia <a href="tel:+543498528914">+54 3498-52-8914</a>
		</p>
		<!-- <p></p>
		<a href="/oficinas">Oficinas</a> -->
		<p><a href="/asociate">Como asociarte</a></p>
	</div>
</div>

<style>
	#info-bar {
		position: fixed;
		top: 0;
		width: 100%;
		background-color: #f6f6f6;
		z-index: 1100;
		transition: transform 0.3s ease;
		height: 40px;
		line-height: 40px;
		padding: 0 1rem;
		display: flex;
		justify-content: center;
		align-items: center;
		text-align: center;
	}

	#info-bar p {
		margin: 0;
		display: flex;
		gap: 0.5rem;
		white-space: nowrap;
		color: #696969;
		font-weight: 500;
	}

	#info-bar a {
		color: inherit;
		text-decoration: underline;
		cursor: pointer;
	}

	#info-bar.hidden {
		transform: translateY(-100%);
	}

	.container {
		display: flex;
		width: 100%;
		justify-content: space-around;
	}
	.icon {
		height: 20px;
		width: 20px;
		margin: auto 0;
	}
</style>

<script>
	document.addEventListener("DOMContentLoaded", () => {
		const infoBar = document.getElementById("info-bar");
		if (!infoBar) return;

		window.addEventListener("scroll", () => {
			const scrollY = window.scrollY;

			if (scrollY === 0) {
				infoBar.classList.remove("hidden");
				requestAnimationFrame(() => {
					window.dispatchEvent(new Event("layoutUpdate"));
				});
			} else {
				infoBar.classList.add("hidden");
				window.dispatchEvent(new Event("layoutUpdate"));
			}
		});
	});
</script>
